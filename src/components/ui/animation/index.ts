/**
 * @file index.ts
 * @role Barrel export for animation components
 * @patterns Barrel Export Pattern
 * @solid SRP (Export management only)
 */

// Animation components
export { default as AnimationProvider } from './AnimationProvider.vue'