/**
 * @file index.ts
 * @role Barrel export for boundary components
 * @patterns Barrel Export Pattern
 * @solid SRP (Export management only)
 */

// Boundary components
export { default as ErrorBoundary } from './ErrorBoundary.vue'
