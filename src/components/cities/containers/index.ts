/**
 * @file index.ts
 * @role Barrel export for city container components
 * @patterns Barrel Export Pattern, Container Pattern
 * @solid SRP (Export management only)
 */

// Export all container components
export { default as CityContainer } from './CityContainer.vue'