/**
 * @file index.ts
 * @role Barrel export for provider components
 * @patterns Barrel Export Pattern
 * @solid SRP (Export management only)
 */

// Provider components
export { default as ClerkProvider } from './ClerkProvider.vue'
export { default as QueryProvider } from './QueryProvider.vue'
