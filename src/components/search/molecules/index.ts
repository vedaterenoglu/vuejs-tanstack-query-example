/**
 * @file index.ts
 * @role Barrel export for search molecule components
 * @patterns Barrel Export Pattern
 * @solid SRP (Export management only)
 */

// Search molecule components
export { default as SearchIconWrapper } from './SearchIconWrapper.vue'